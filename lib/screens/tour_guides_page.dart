import 'package:flutter/material.dart';

class TourGuidesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Tour Guides'),
      ),
      body: Center(
        child: Text('Tour Guides Page'),
      ),
    );
  }
}
